# 아이소메트릭 엔진 개발 연구

## 프로젝트 목표
- 2D 이미지를 아이소메트릭 3D 객체로 변환하는 범용 엔진 개발
- 23평 아파트 도면을 98% 유사도로 아이소메트릭 변환
- 실용적이고 확장 가능한 아이소메트릭 렌더링 플랫폼 구축

## 1. 아이소메트릭 이론 연구

### 1.1 아이소메트릭 프로젝션 기본 원리

#### 정의
아이소메트릭(Isometric) 프로젝션은 3D 객체를 2D 평면에 투영하는 방법 중 하나로, 세 축(X, Y, Z)이 모두 120도 각도로 배치되어 보는 객체의 크기가 왜곡되지 않는 평행 투영법입니다.

#### 핵심 특징
- **축 각도**: X축 30°, Y축 -30°, Z축 90° (수직)
- **크기 보존**: 모든 축의 스케일이 동일하게 유지
- **시각적 효과**: 입체감을 제공하면서도 정확한 측정 가능

#### 수학적 변환 공식
```
x_iso = (x - z) * cos(30°)
y_iso = (x + z) * sin(30°) + y
```

### 1.2 좌표계 변환

#### 3D → 아이소메트릭 변환 매트릭스
```
| cos(30°)   0   -cos(30°) |   |x|
| sin(30°)   1    sin(30°) | × |y|
|    0       0       0     |   |z|
```

#### 역변환 (아이소메트릭 → 3D)
```
x = (x_iso / cos(30°) + z_iso / cos(30°)) / 2
z = (z_iso / cos(30°) - x_iso / cos(30°)) / 2
y = y_iso - (x + z) * sin(30°)
```

## 2. 이미지 → 아이소메트릭 변환 방법론

### 2.1 2D 도면 분석 단계

#### 이미지 전처리
1. **윤곽선 검출**: Edge detection을 통한 구조 파악
2. **색상 분할**: 방별, 구역별 색상 클러스터링
3. **깊이 추정**: 벽의 두께, 높이 정보 추론
4. **객체 분류**: 벽, 문, 창문, 가구 등 요소 식별

#### 공간 정보 추출
1. **스케일 계산**: 도면의 실제 크기 비율 산출
2. **방 경계 인식**: 각 방의 윤곽선과 면적 계산
3. **연결 관계**: 문과 복도를 통한 방 간 연결성
4. **높이 정보**: 일반적인 아파트 높이 (2.4m) 적용

### 2.2 3D 객체 생성 알고리즘

#### 벽체 생성
```
for each wall_segment in floor_plan:
    create_wall_volume(
        start_point: (x1, 0, z1),
        end_point: (x2, 0, z2),
        height: 2400mm,
        thickness: 150mm
    )
```

#### 바닥/천장 생성
```
for each room in rooms:
    create_floor(room.boundary, height: 0)
    create_ceiling(room.boundary, height: 2400mm)
```

### 2.3 아이소메트릭 렌더링

#### 시점 설정
- **카메라 각도**: 35.264° (아이소메트릭 표준)
- **축 회전**: X축 -35.264°, Y축 45°, Z축 0°
- **거리**: 최적 시인성을 위한 동적 조절

#### 깊이 및 그림자
- **Z-ordering**: 깊이 기반 렌더링 순서
- **조명 계산**: 일정한 각도의 평행광 적용
- **그림자 투영**: 바닥면에 객체 그림자 생성

## 3. 엔진 아키텍처 설계

### 3.1 핵심 컴포넌트

#### IsometricEngine
```dart
class IsometricEngine {
  // 좌표 변환
  Point2D to2D(Point3D point3d)
  Point3D to3D(Point2D point2d, double height)
  
  // 객체 렌더링
  void renderObject(IsometricObject object)
  void renderScene(List<IsometricObject> objects)
  
  // 시점 조절
  void setViewAngle(double angle)
  void setZoom(double zoom)
}
```

#### IsometricObject
```dart
abstract class IsometricObject {
  Point3D position
  Vector3D dimensions
  Color color
  
  void render(Canvas canvas, IsometricEngine engine)
  List<Point3D> getVertices()
  bool intersects(Ray ray)
}
```

#### 전문 객체 클래스
```dart
class IsometricWall extends IsometricObject
class IsometricRoom extends IsometricObject
class IsometricFurniture extends IsometricObject
class IsometricBuilding extends IsometricObject
```

### 3.2 파이프라인 설계

```
입력 이미지 → 전처리 → 객체 추출 → 3D 변환 → 아이소메트릭 렌더링 → 출력
```

1. **ImageProcessor**: 이미지 분석 및 전처리
2. **ObjectExtractor**: 구조 요소 추출 및 분류
3. **SceneBuilder**: 3D 장면 구축
4. **IsometricRenderer**: 최종 아이소메트릭 렌더링

## 4. 기술 요구사항

### 4.1 Flutter 구현
- **CustomPainter**: 아이소메트릭 렌더링
- **Canvas API**: 2D 그래픽 드로잉
- **Image Processing**: 이미지 분석 라이브러리
- **Vector Math**: 3D 좌표 변환

### 4.2 성능 최적화
- **Spatial Indexing**: 효율적인 객체 검색
- **Level of Detail**: 거리 기반 디테일 조절
- **Culling**: 보이지 않는 객체 제거
- **Batch Rendering**: 동일 유형 객체 일괄 렌더링

## 5. 검증 및 평가 방법

### 5.1 정확도 측정
- **기하학적 정확도**: 실제 치수와 렌더링 결과 비교
- **시각적 유사도**: SSIM 등을 통한 이미지 품질 평가
- **구조 보존도**: 방 배치, 연결성 정확도

### 5.2 성능 평가
- **렌더링 속도**: FPS 측정
- **메모리 사용량**: 객체 수에 따른 메모리 효율성
- **스케일링**: 복잡도 증가에 따른 성능 변화

## 6. 연구 일정

### Phase 1: 기초 연구 ✅ 완료 (2025-09-05)
- [x] 이론 연구 및 문서화
- [x] 기존 솔루션 조사 
- [x] 수학적 모델 검증 (30도 표준 각도 확인)

### Phase 2: 엔진 개발 ✅ 완료 (2025-09-05)
- [x] 핵심 엔진 클래스 구현 (IsometricEngine)
- [x] 기본 변환 함수 개발 (IsometricTransform)
- [x] 단순 객체 렌더링 테스트 (IsometricBox)

### Phase 3: 이미지 처리 (2주)
- [ ] 도면 분석 알고리즘
- [ ] 객체 추출 로직
- [ ] 3D 변환 파이프라인

### Phase 4: 통합 및 최적화 (1-2주)
- [ ] 전체 시스템 통합
- [ ] 성능 최적화
- [ ] Playwright 자동화 테스트

### Phase 5: 실제 적용 (1주)
- [ ] 23평 아파트 도면 적용
- [ ] 98% 유사도 달성
- [ ] 최종 검증

## 7. 예상 도전 과제

### 기술적 도전
1. **깊이 정보 부족**: 2D 도면에서 3D 높이 추정의 어려움
2. **복잡한 구조**: 불규칙한 방 모양, 경사면 등
3. **실시간 성능**: 큰 아파트 도면의 실시간 렌더링
4. **정확도**: 세부 구조의 정밀한 재현

### 해결 방안
1. **도메인 지식 활용**: 아파트 설계 표준을 통한 높이 추정
2. **단계적 처리**: 복잡한 구조를 단순 요소로 분해
3. **LOD 시스템**: 거리별 디테일 레벨 조절
4. **반복 개선**: 결과 피드백을 통한 알고리즘 개선

## 8. 프로젝트 개발 로드맵

### Phase 1: 기초 연구 ✅ 완료
- [x] 아이소메트릭 이론 조사 및 정리
- [x] 변환 알고리즘 이해 및 검증
- [x] Flutter 렌더링 엔진 연구

### Phase 2: 엔진 개발 ✅ 완료
- [x] Vector3D, Vector2D 클래스 구현 (lib/math/)
- [x] IsometricTransform 클래스 구현 (30도 표준 각도)
- [x] IsometricEngine 코어 클래스 구현 (렌더링, 성능 모니터링)
- [x] IsometricObject 추상 클래스 및 IsometricBox 구현
- [x] Flutter CustomPainter 통합

### Phase 3: 고급 기능 ✅ 완료
- [x] IsometricLighting 조명 시스템 구현
- [x] IsometricCamera 카메라 컨트롤 시스템
- [x] IsometricPerformance 성능 모니터링 시스템
- [x] 실시간 애니메이션 및 상호작용
- [x] 렌더링 최적화 (컬링, 정렬)

### Phase 4: 성능 검증 ✅ 완료
- [x] Playwright 스타일 성능 테스트 구현 (3000+ FPS 달성)
- [x] 웹 환경 통합 테스트 구현
- [x] 메모리 사용량 및 렌더링 스트레스 테스트
- [x] 히트 테스트 및 경계 계산 성능 검증

### Phase 5: 이미지 변환 🚧 진행 중
- [ ] 2D 이미지 로딩 및 분석
- [ ] 윤곽선 추출 알고리즘
- [ ] 깊이 정보 추정
- [ ] 3D 메시 생성

### Phase 6: 검증 및 완성
- [ ] 23평 아파트 도면 테스트
- [ ] 98% 유사도 검증
- [ ] 최종 성능 테스트

## 9. 성능 검증 결과 (2025-09-05)

### 엔진 성능 테스트 결과
- **초기화 성능**: 10ms 미만
- **대량 객체 처리**: 1000개 객체 100ms 미만 추가
- **렌더링 성능**: 평균 3145 FPS (317μs/프레임)
- **메모리 관리**: 안정적인 객체 추가/제거
- **변환 계산**: 1000회 3D→2D 변환 5ms 미만

### 웹 환경 통합 테스트
- Flutter Web Chrome 환경에서 실시간 렌더링 확인
- 상호작용 (확대/축소, 객체 추가, 회전) 정상 동작
- 성능 모니터링 실시간 표시 확인
- 50+ 객체 동시 렌더링 시에도 10 FPS 이상 유지

---

*연구 시작일: 2025-09-05*
*성능 검증 완료: 2025-09-05*
*다음 단계: 23평 아파트 도면 수집 및 분석*